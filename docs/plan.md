# План работ

1. Сформировать бизнес-требования в `docs/requirements.md` — выполнено
2. Обновить описание архитектуры в `docs/architecture.md` — выполнено
3. Актуализировать `.env.example`, `.env` и шаблон переменных окружения — выполнено
4. Добавить и обновить тесты в `tests` — выполнено
5. Обновить `README.md` с инструкцией по развертыванию на сервере — выполнено
6. Перевести OpenAI интеграцию на Responses API и вынести промпты в файлы — выполнено
7. Добавить тесты критичных сценариев — выполнено
8. Добавить поддержку уровня размышления для моделей OpenAI — выполнено
9. Добавить поддержку температуры для моделей OpenAI — выполнено

10. Детально актуализировать документацию проекта (README, architecture, env template) — выполнено

## Новый план по дополнениям (статусы incomplete/complete)

11. Подготовка схемы данных Google Sheets — выполнено
- Добавить/зафиксировать новые статусы: `Revised (incomplete)` и `Revised (complete)`.
- Добавить служебные колонки (если отсутствуют):
  - `WP PRODUCT ID EN` (ID EN-товара в WooCommerce для обновлений),
  - `WP PRODUCT ID PT` (ID PT-товара в WooCommerce для обновлений),
  - `WEBSITE SNAPSHOT HASH` (контрольная сумма snapshot),
  - `LAST DIFF CHECK AT` (время последней проверки изменений).
- Зафиксировать рабочие статусы `Revised (incomplete)`, `Revised (complete)`, `Published (incomplete)` и `Published`.

12. Реорганизация пайплайна статусов в `main.py` — выполнено
- Разделить обработку строк на отдельные сценарии:
  - сценарий A: `Revised (incomplete)`,
  - сценарий B: `Revised (complete)`,
  - сценарий C: `Published (incomplete)` (только мониторинг WEBSITE + уведомления).
- Вынести маршрутизацию сценариев в отдельные функции для тестируемости.

13. Сценарий публикации `Revised (incomplete)` — выполнено
- Генерировать и записывать поля:
  `ORG INFO`, `ORG INFO (PT)`, `SUMMARY`, `SUMMARY (PT)`, `BENEFITS`, `BENEFITS (PT)`, `FAQ`, `FAQ (PT)`.
- Реализовать baseline hash WEBSITE на день публикации:
  - вычислить hash после нормализации HTML,
  - записать hash в таблицу.
- После успешной публикации выставлять `Published (incomplete)`.

14. Ежедневный мониторинг изменений WEBSITE — выполнено
- Для строк со статусом `Published (incomplete)` и заполненным baseline hash:
  - загружать текущую версию страницы,
  - сравнивать с сохранённой (текст, ссылки, изображения, другие HTML-изменения),
  - формировать структурированный diff (кратко + детали).
- Логировать результат проверки и обновлять `LAST DIFF CHECK AT`.

15. Интеграция уведомлений в Telegram — выполнено
- Добавить отправку уведомления в Telegram-группу при обнаружении изменений WEBSITE.
- Вынести Telegram-клиент в отдельный модуль с retry/timeout и обработкой ошибок.
- Добавить env-переменные:
  - `TELEGRAM_API_ID`,
  - `TELEGRAM_API_HASH`,
  - `TELEGRAM_SESSION_NAME`,
  - `TELEGRAM_TARGET`,
  - `TELEGRAM_NOTIFICATIONS_ENABLED`.
- Формат сообщения: ID гонки, название, ссылка на WEBSITE, факт изменений, краткое summary diff.

16. Сценарий `Revised (complete)` для генерации и перезаписи — выполнено
- Генерировать поля:
  `ORG INFO`, `ORG INFO (PT)`, `SUMMARY`, `SUMMARY (PT)`, `BENEFITS`, `BENEFITS (PT)`, `FAQ`, `FAQ (PT)`.
- Записывать сгенерированные поля в таблицу.
- Обновлять существующий товар в WordPress по `WP PRODUCT ID EN` и `WP PRODUCT ID PT`.
- Правило частичного обновления: если в таблице поле пустое, существующее значение в WordPress не удалять.
- После обновления выставлять `Published`.

17. Повторный цикл `Published -> Revised (complete)` — выполнено
- При повторной установке `Revised (complete)`:
  - снова генерировать 8 полей,
  - перезаписывать их в таблице,
  - повторно обновлять эти же данные в WordPress.
- Обеспечить идемпотентность и безопасные повторные запуски.

18. Изменения в модуле WordPress-интеграции — выполнено
- Добавить режим `create_or_update`:
  - создание при отсутствии `WP PRODUCT ID EN`/`WP PRODUCT ID PT`,
  - обновление при наличии `WP PRODUCT ID EN`/`WP PRODUCT ID PT`.
- Добавить selective update payload (передавать только непустые поля).
- Сохранение `WP PRODUCT ID EN` и `WP PRODUCT ID PT` в таблицу после первого успешного создания.

19. Хранилище baseline hash и ссылки — выполнено
- Сохранять в таблице только baseline hash без хранения HTML-файлов.
- Использовать нормализацию HTML перед hash, чтобы снизить ложные срабатывания.

20. Тестирование — выполнено
- Добавить unit-тесты:
  - маршрутизация статусов,
  - сценарий incomplete с генерацией полей,
  - перевод статуса в `Published (incomplete)` после incomplete-публикации,
  - мониторинг только для `Published (incomplete)`,
  - сценарий complete с генерацией и selective update,
  - baseline hash и проверка изменений,
  - diff-детектор,
  - Telegram-уведомления (mock API),
  - повторный цикл `Published -> Revised (complete)`.
- Добавить интеграционные тесты на обработку строки с `WP PRODUCT ID EN`/`WP PRODUCT ID PT` и без них.

21. Обновление конфигурации и документации — выполнено
- Актуализировать `.env.example`, `.env`, `env.template` (новые TELEGRAM-переменные).
- Обновить `docs/architecture.md` и `README.md` под новый двухэтапный flow.
- Обновить `docs/requirements.md` с новыми бизнес-правилами статусов и мониторинга.

22. Релиз и контроль внедрения — запланировано
- Прогон в Docker (локально/сервер) с тестовым набором строк.
- Проверка логов, проверка уведомлений Telegram, проверка обновлений в WP.
- Фиксация результата по каждому пункту этого плана в `docs/plan.md` со статусом `выполнено` после реализации.

23. Синхронизация вариаций по ID (`WP VARIATION ID EN/PT`) — выполнено
- Добавлена полная синхронизация вариаций EN/PT в режиме `Revised (complete)`:
  - update вариаций при наличии ID в таблице,
  - create вариаций при отсутствии ID,
  - delete вариаций в WooCommerce, отсутствующих в текущем наборе таблицы.
- Реализована запись актуальных `WP VARIATION ID EN` и `WP VARIATION ID PT` обратно в строки вариаций Google Sheets.

24. Корректировка генерации для `Revised (incomplete)` — выполнено
- Для статуса `Revised (incomplete)` включена генерация и запись полей `ORG INFO`, `ORG INFO (PT)`, `SUMMARY`, `SUMMARY (PT)`, `BENEFITS`, `BENEFITS (PT)`, `FAQ`, `FAQ (PT)` по тому же сценарию, что и для `Revised (complete)`.
- Для `Revised (complete)` подтверждена перегенерация этих полей при повторном запуске.

25. Удаление legacy-статуса `Revised` — выполнено
- Из кода удалена отдельная обработка `Revised`.
- В рабочем процессе используются только `Revised (incomplete)`, `Revised (complete)`, `Published (incomplete)` и `Published`.

26. Recovery-сценарий восстановления старых WP ID — выполнено
- Добавлен ручной скрипт `run/recover_wp_ids.py` с режимами `dry-run` и `apply`.
- Реализовано восстановление `WP PRODUCT ID EN/PT` по `LINK RACEFINDER`, публичным страницам и `hreflang="pt-pt"`.
- Реализовано восстановление `WP VARIATION ID EN/PT` по нормализованному ключу вариации.
- Добавлена защита от неоднозначных совпадений и запрет перезаписи уже заполненных ID.
- Обновлены тесты, env-шаблоны, README и архитектурная документация.
